let axios,type,argv;_eb6‍.x([["Admin_SessionUpdate",()=>Admin_SessionUpdate]]);_eb6‍.w("axios",[["default",["axios"],function(v){axios=v}]]);_eb6‍.w("os",[["type",["type"],function(v){type=v}]]);_eb6‍.w("process",[["argv",["argv"],function(v){argv=v}]]);


var fs = require('fs')
var querystring = require('querystring')
var FormData = require('form-data');

       async function Admin_SessionUpdate(args, token){

    let myArray = process.argv.slice(3)

    if (typeof args._[1] != 'undefined'){
        console.log('You used extra parameters')
        return
    }

    if (typeof args.source === 'boolean' || typeof args.source === 'undefined'){
        console.log('You have to set parameter --source')
        return
    }

    var url = 'http://localhost:8765/evcharge/api/admin/sessionsupd/'
    //url = url + args.username + '/' + args.passw

    var header = {'x-observatory-auth' : token,
                  'Content-Type' : 'multipart/form-data'} 

    var filename = __dirname + '/' + args.source
    _eb6‍.g.console.log(filename) 
    var newfile = fs.createReadStream(filename)
    const form_data = new FormData()
    form_data.append("file", newfile)
    
    function request(url){
        axios.post(url,   //add stuff here
        {headers : header})
        .then((response => {
            _eb6‍.g.console.log(response.data.message)
        }))
        .catch((error) => {
            _eb6‍.g.console.log("Error", error.response.status)
            _eb6‍.g.console.log(error.response.data.message)
        })
    }
    request(url)
    return
}