let axios;_9f4‍.x([["Logout",()=>Logout]]);_9f4‍.w("axios",[["default",["axios"],function(v){axios=v}]]);
var fs = require('fs')


       async function Logout(args){
    
    var url = 'http://localhost:8765/evcharge/api/logout'
    var token_path = './softeng20bAPI.token'
    
    let myArray = process.argv.slice(2)
    if (myArray.length > 1){
        console.log('In order to logout just type logout')
        return
    }
    
    function request(url, path){   
        fs.readFile(path, 'utf8',
        function post_request(err, data){
            if (err){
                console.log('Error when trying to read the file')
                return
            }
            var token = data
            data = {}
            axios.post(url, data,
            {headers : {
                'x-observatory-auth' : token,
                'Content-Type' : 'application/x-www-form-urlencoded'
            }})
            .then(response => {
                _9f4‍.g.console.log("Status",response.status)
                _9f4‍.g.console.log(response.data)
                fs.unlink(path, (err) => {
                    if (err){
                        _9f4‍.g.console.error(err, 'Error while deleting the file')
                        return
                    }
                })
            })
            .catch((error) => {
                _9f4‍.g.console.log("Error", error.response.status)
                _9f4‍.g.console.log(error.response.data.message)
            })
        })
    }

    fs.access(token_path, fs.F_OK, (err) => {
        if (err){
            console.error('You need to log in first')
            return
        }
        request(url, token_path)
    })
    return
}
